---
groups:
# Applications & Services
  - name: application_servers
    groups:
      # Kubernetes Primary Nodes
      - name: k8s-primary
        targets: 
          - 1.1.1.1

      - name: k8s-nodes
        targets:
          - 192.168.1.20

# General Config
config:
  ssh:
    user: ubuntu
    host-key-check: false
    private-key: ~/.ssh/dan-luxmc